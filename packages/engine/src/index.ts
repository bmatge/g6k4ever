/**
 * @g6k4ever/engine — moteur d'évaluation TS PUR.
 *
 * Phase 3 du plan.
 * Aucune dépendance React, DOM, ou HTTP. PAS d'eval().
 * Voir l'agent `engine-dev` dans .claude/agents/engine-dev.md.
 *
 * Signature mentale :
 *   (définition, entrées, résolveurs) → (état visible, variables, erreurs, sorties)
 */

export const ENGINE_PACKAGE_VERSION = "0.0.0" as const;
