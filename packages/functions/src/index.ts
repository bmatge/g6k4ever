/**
 * @g6k4ever/functions — registre des fonctions.
 *
 * Phase 3 du plan.
 * Fonctions standard (sum, floor, max, count, year, strftime, defined...) +
 * fonctions métier injectables par les développeurs (workdaysofmonth, getInsee...).
 */

export const FUNCTIONS_PACKAGE_VERSION = "0.0.0" as const;
