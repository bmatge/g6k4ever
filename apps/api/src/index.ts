/**
 * @g6k4ever/api — API Hono + SQLite.
 *
 * Phase 5 du plan.
 * Zéro logique métier (tout via @g6k4ever/engine).
 * Voir l'agent `api-dev` dans .claude/agents/api-dev.md.
 */

export const API_PACKAGE_VERSION = "0.0.0" as const;
