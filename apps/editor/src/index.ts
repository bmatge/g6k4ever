/**
 * @g6k4ever/editor — back-office no-code React + react-dsfr.
 *
 * Phase 7 du plan. C'est le produit.
 * Mode guidé ⇄ expert, preview live, vérificateur de cohérence, lock + versions.
 * Voir l'agent `editor-dev` dans .claude/agents/editor-dev.md.
 */

export const EDITOR_PACKAGE_VERSION = "0.0.0" as const;
