{
  "name": "@g6k4ever/runtime",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Runtime React + react-dsfr. Rend une définition. Buildable en bundle autonome embeddable. Phase 6.",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "build:app": "vite build",
    "build:lib:embedded": "G6K_RUNTIME_MODE=embedded vite build --config vite.lib.config.ts",
    "build:lib:standalone": "G6K_RUNTIME_MODE=standalone vite build --config vite.lib.config.ts",
    "build:lib": "pnpm build:lib:embedded && pnpm build:lib:standalone",
    "measure:embedded": "pnpm build:lib:embedded && node scripts/measure-bundle.mjs embedded",
    "measure:standalone": "pnpm build:lib:standalone && node scripts/measure-bundle.mjs standalone",
    "measure": "pnpm measure:embedded && pnpm measure:standalone",
    "dev": "vite",
    "preview": "vite preview",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@codegouvfr/react-dsfr": "^1.32.2",
    "@g6k4ever/blocks": "workspace:*",
    "@g6k4ever/engine": "workspace:*",
    "@g6k4ever/functions": "workspace:*",
    "@g6k4ever/schema": "workspace:*",
    "react": "^19.2.6",
    "react-dom": "^19.2.6"
  },
  "devDependencies": {
    "@testing-library/dom": "^10.4.1",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "@types/react": "^19.2.15",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.2",
    "jsdom": "^29.1.1",
    "vite": "^8.1.5"
  }
}
